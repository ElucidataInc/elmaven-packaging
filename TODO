- display at what stage the installer is
- display what branch the installer is being build for 
- use a log file and store the output/error of commands in that file
- uploading of symbols might fail. look into this
